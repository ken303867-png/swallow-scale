name: Validate backup fix
on:
  push:
    branches: [feature/transfer-fix]
permissions:
  contents: read
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Validate files
        run: |
          node --check data-transfer.js
          grep -q "dysphagia_scale_drill_v1" data-transfer.js
          grep -q "swallow-scale-v3-backup" sw.js
          ! grep -q "localStorage.clear" data-transfer.js
